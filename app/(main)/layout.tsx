import { currentUser } from "@/app/actions/helpers"; import { Header } from "@/components/Header"; import { redirect } from "next/navigation";
export default async function MainLayout({ children }: { children: React.ReactNode }) { const user = await currentUser(); if (!user.username && !children) redirect("/profile"); return <><Header username={user.username || "Profile"} admin={user.role === "admin"} /><main className="mx-auto max-w-4xl px-4 py-8">{children}</main></>; }
