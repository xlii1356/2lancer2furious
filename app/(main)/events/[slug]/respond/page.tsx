import { notFound } from "next/navigation"; import { db } from "@/db"; import { events, responses } from "@/db/schema"; import { and, eq } from "drizzle-orm"; import { currentUser } from "@/app/actions/helpers"; import { saveResponse } from "@/app/actions/responses";
export default async function Respond({ params }: { params: Promise<{ slug: string }> }) { const user = await currentUser(); const slug = (await params).slug; const event = await db.query.events.findFirst({ where: eq(events.slug, slug) }); if (!event) notFound(); const response = await db.query.responses.findFirst({ where: and(eq(responses.eventId, event.id), eq(responses.authorId, user.id)) }); const text = ((response?.body as { content?: { content?: { text?: string }[] }[] })?.content || []).map((p) => p.content?.map((n) => n.text || "").join("") || "").join("\n\n"); async function submit(data: FormData) { "use server"; const body = { type: "doc", content: String(data.get("text") || "").split(/\n{2,}/).filter(Boolean).map((value) => ({ type: "paragraph", content: [{ type: "text", text: value }] })) }; data.set("body", JSON.stringify(body)); await saveResponse(data); } return <><h1 className="text-3xl font-bold">Your response</h1><form action={submit} className="mt-6 space-y-4"><input type="hidden" name="eventId" value={event.id}/><input type="hidden" name="slug" value={slug}/><label>Posted as<input name="byline" defaultValue={response?.byline || user.username || ""} maxLength={60}/></label><label>Response<textarea name="text" defaultValue={text} rows={14}/></label><button>{response ? "Update response" : "Post response"}</button></form></>; }
