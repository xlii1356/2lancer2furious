import { currentUser } from "@/app/actions/helpers"; import { saveProfile } from "@/app/actions/profile"; import { ImageUpload } from "@/components/ImageUpload";
export default async function ProfilePage() { const user = await currentUser(); return <><h1 className="text-3xl font-bold">Your profile</h1><form action={saveProfile} className="mt-6 max-w-lg space-y-4"><label>Username<input name="username" defaultValue={user.username || ""} required minLength={2} maxLength={24} /></label><label>Avatar<ImageUpload name="avatarUrl" defaultValue={user.avatarUrl} endpoint="/api/avatar-upload" /></label><button>Save profile</button></form></>; }
