"use server";
import { db } from "@/db"; import { responses } from "@/db/schema"; import { and, eq } from "drizzle-orm"; import { currentUser, requireAdmin } from "./helpers"; import { revalidatePath } from "next/cache"; import { redirect } from "next/navigation";
export async function saveResponse(formData: FormData) { const user = await currentUser(); const eventId = String(formData.get("eventId")); const slug = String(formData.get("slug")); const byline = String(formData.get("byline") || "").trim().slice(0, 60) || null; const body = JSON.parse(String(formData.get("body"))); await db.insert(responses).values({ eventId, authorId: user.id, byline, body }).onConflictDoUpdate({ target: [responses.eventId, responses.authorId], set: { byline, body, updatedAt: new Date() } }); revalidatePath(`/events/${slug}`); redirect(`/events/${slug}`); }
export async function deleteResponse(id: string, slug: string) { const user = await currentUser(); const response = await db.query.responses.findFirst({ where: eq(responses.id, id) }); if (!response || (response.authorId !== user.id && user.role !== "admin")) throw new Error("Not authorized"); await db.delete(responses).where(eq(responses.id, id)); revalidatePath(`/events/${slug}`); }
