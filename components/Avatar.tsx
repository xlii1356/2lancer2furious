import Image from "next/image";
export function Avatar({ id, url, name, size = 40 }: { id: string; url: string | null; name: string | null; size?: number }) { if (url) return <Image className="rounded-full object-cover" src={url} alt="" width={size} height={size} />; const initials = (name || "?").split(/\s+/).map((p) => p[0]).join("").slice(0, 2).toUpperCase(); const hue = [...id].reduce((sum, c) => sum + c.charCodeAt(0), 0) % 360; return <span aria-label={name || "Member"} className="inline-flex shrink-0 items-center justify-center rounded-full font-semibold text-white" style={{ width: size, height: size, backgroundColor: `hsl(${hue} 42% 44%)` }}>{initials}</span>; }
